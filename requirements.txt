# Core
python-dotenv>=1.0.1

# LangChain ecosystem
langchain>=1.0.0   # bumped from 0.3.0: Phase 6/8 rely on current langchain.agents.middleware API
langchain-core>=0.3.0
deepagents>=0.7.0   # Phase 8 — create_deep_agent, TodoListMiddleware, filesystem/subagent tools
langchain-openai>=0.2.0
langchain-anthropic>=0.2.0
langchain-community>=0.4.2
langchain-classic>=1.0.8   # Phase 4 — retrievers/storage moved out of core langchain in v1 (MultiQueryRetriever, EnsembleRetriever, ParentDocumentRetriever, InMemoryStore, ...)
langgraph>=1.0.0   # Phase 6 relies on InMemorySaver, interrupt()/Command, RetryPolicy, @entrypoint/@task
langsmith>=0.8.0   # bumped from 0.1.100: Phase 9 verified against installed 0.8.5

# Embeddings / vector stores (Phase 0-4)
chromadb>=0.5.0
langchain-chroma>=0.1.4
faiss-cpu>=1.8.0
tiktoken>=0.7.0

# Document loading / splitting (Phase 3+)
pypdf>=4.2.0
langchain-text-splitters>=0.3.0
beautifulsoup4>=4.12.0
reportlab>=4.0.0   # generates a real-text sample PDF for Phase 3 Topic 07's demo

# Tools used in examples
duckduckgo-search>=6.2.0
rank-bm25>=0.2.2   # Phase 4 — scoring backend for BM25Retriever (hybrid/keyword search)

# Phase 2 — optional provider integrations (install only the ones you'll demo)
# langchain-google-genai>=2.0.0   # Gemini (Topic 02)
# langchain-ollama>=0.2.0         # local models (Topic 03) — needs Ollama running locally

# Phase 10 — production API layer
fastapi>=0.111.0
uvicorn>=0.30.0
psycopg[binary]>=3.2.0
redis>=5.0.0
